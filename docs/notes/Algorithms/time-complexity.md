P:  Polynomial Time Complexity, 多项式时间复杂度, 可以写作 $ n^{O(1)} $ 的形式
EXP: 可以在指数时间内解决的问题, 比如 $\mathbb{R} ^ {n^{O(1)}} $, 底数为常数, 指数为多项式
NP: Nondeterministic Polynomial time, 
给定一个候选解(witness), 一个 checker 的复杂度是  $ n^{O(1)} $ 的
R: 所有能在有限时间内求解的判定问题 (递归语言 / decidable language)


