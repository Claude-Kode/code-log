# 算法技能树 · GoryK

> 基于 `templates/TEMPLATE-FUNCTIONS.md` 和实际做题记录整理。
> 标记规则：
> - ✅ = 能闭卷手写，基础应用没问题
> - ⭐ = 高级用法偶尔需对照模板
> - 🚧 = 正在学 / 只会用不会写
> - ❌ = 没接触过

---

## 一、基础功（全部 ✅）

```
基础功
├── 位运算 ──── lowbit / popcount / ctz / clz / __builtin_
├── 离散化 ──── sort + unique + lower_bound / getId / ranked
├── 随机数 ──── mt19937 / uniform_int_distribution
├── 浮点处理 ── eps 判等 / 大数溢出意识
└── 对拍 ──── generator + brute + fc 比较 + TLE 检测
```

---

## 二、数学（全部 ✅）

```
数学
├── 快速幂 ───── qpow(a, b, mod)
├── 乘法逆元 ──── inv(x) = qpow(x, mod-2, mod)
├── 线性筛 ───── primes + spf 最小质因子
├── 组合数 ───── 阶乘 + 逆元预处理 / C(n, m) O(1)
├── mint 类 ──── 运算符重载 + 自动取模
├── 最大公约数 ── gcd / lcm
└── 简单数论 ─── 质因数分解 / 约数个数 / 约数和
```

---

## 三、数据结构 — 并查集家族（全部 ✅）

```
并查集
├── 基础 DSU ────── find(x) + unite(x, y) + 路径压缩 + 按秩合并
├── 带权 DSU ────── d[x] 记录与根的相对关系（偏移量）
├── 回滚 DSU ────── 可撤销合并 / 离线 + 时间轴
└── 应用 ───────── Kruskal 最小生成树 / 判环 / 动态连通性
```

---

## 四、数据结构 — 树状数组（全部 ✅）

```
树状数组 (BIT)
├── 单点加 + 前缀和
├── 区间加 + 单点查 ─── 差分
└── 二维 BIT ────────── 矩阵子矩阵和（模板里有）
```

---

## 五、数据结构 — 线段树家族

```
线段树
├── 基础线段树 ──── 单点改 + 区间查 ✅
├── 懒标记线段树 ── 区间加/乘 + 区间和/最值 ⭐
│   ├── Info / Tag 协议
│   ├── apply() / operator+()
│   ├── pushdown / pushup
│   └── [复杂 Tag 组合时偶尔对照]
├── 动态开点 ──── ✅
└── 权值线段树 ── ✅
```

---

## 六、数据结构 — 可持久化

```
可持久化
└── 主席树 ──── 历史版本 / 静态区间第 k 小 ⭐
    ├── 动态开点
    ├── 前缀和版本链
    └── [能套模板过题]
```

---

## 七、数据结构 — 分块 / 根号算法

```
分块
├── 数列分块入门 1：区间加 + 单点查 ✅
├── 数列分块入门 2：区间加 + 区间小于某数的个数 ✅
├── 数列分块入门 3：区间加 + 区间前驱/后继 ✅
├── 数列分块入门 4：区间加 + 区间和 ✅
└── 莫队算法 ───── ❌（没写过）
```

---

## 八、数据结构 — 高级 / 特定场景

```
高级数据结构
├── ODT（珂朵莉树） ─── set 维护区间 / 区间赋值 + 值域随机 ✅
├── ST 表 ──────────── 静态 RMQ / 不可变数组 O(1) 查询 ✅
├── Trie ──────────── 前缀树 / 插入 + 查找 + 最大异或 ✅
│   └── 0-1 Trie ──── 异或极值 ✅
├── pb_ds ───────────
│   ├── order_of_key / find_by_order ✅
│   ├── 可改堆 ✅
│   └── hash 表（防 hack）✅
├── 单调栈 / 单调队列 ─ 滑动窗口最值 / 下一个更大元素 ✅
└── 树上启发式合并 (DSU on tree) ── 模板里有 ✅
```

---

## 九、图论（全部 ✅）

```
图论
├── 链式前向星 ──── 邻接表存储 / idx 管理边
├── 最短路
│   ├── Dijkstra ──── 优先队列优化 / 非负权图首选
│   ├── SPFA ──────── 含负权尝试 / 易被卡 TLE
│   └── Floyd ─────── 全源 / n≤500
├── 最小生成树
│   ├── Prim ──────── 邻接矩阵 / 邻接表 + 堆
│   └── Kruskal ───── DSU + 排序边
├── LCA ──────────── 树上最近公共祖先（倍增法）
├── 拓扑排序 ────── BFS(Kahn) / 判环
├── 二分图染色 ──── 判二分图
└── 桥/割点 ─────── Tarjan（会用）
```

---

## 十、字符串

```
字符串
├── KMP ──────────── next 数组 + 匹配 ✅（C++ + Python 双版本）
├── 字符串哈希 ──── 自然溢出 / 双哈希 ✅
├── AC 自动机 ──── ❌
├── Manacher ────── ❌
├── Z 函数 ──────── ❌
└── 后缀数组 ────── ❌
```

---

## 十一、动态规划（全部 ✅，基础应用没问题）

```
DP
├── 线性 DP ────── LIS / LCS / 编辑距离 / 最大子段和
├── 区间 DP ────── 石子合并 / 矩阵链乘 / 区间划分
├── 背包 DP ────── 01 / 完全 / 多重 / 分组
├── 树形 DP ────── 树上选点 / 树背包 / 换根
├── 状压 DP ────── 枚举子集 / 哈密顿路径 / 状态压缩
└── 数位 DP ────── 模板里有 ✅
```

---

## 十二、贪心 / 搜索（全部 ✅）

```
搜索
├── DFS + 回溯 ─── 排列 / 组合 / 剪枝
├── BFS ──────── 最短路（无权图）/ 状态搜索 / Flood Fill
├── 二分答案 ─── while(l < r) 板子
└── 贪心 ──────── 排序贪心 / 反证法 / 交换论证
```

---

## 十三、调试与工具链

```
工具链
├── freopen ─────── stdin/stdout/stderr 重定向 ✅
├── #ifdef LOCAL ── 本地/评测环境自动切换 ✅
├── dbug 宏 ─────── 打印变量名 + 值 ✅
├── __DEBUG_TOOL.h ─ 使用：debug() / REGISTER_REFLECT 🚧
│   └── [AI 写的，会用，源码只部分理解]
├── 对拍系统 ─────
│   ├── generator.cpp ── 造数据 ✅
│   ├── brute.cpp ────── 暴力 ✅
│   ├── wa.cpp ───────── 待测程序 ✅
│   └── fc 比较 ──────── ✅
├── g++ 编译参数 ── -std=c++14 -O2 -Wall -DLOCAL ✅
└── CMake ──────── 了解概念，未实践 ❌
```

---

## 十四、能力总表

| 领域 | 题量/经验 | 自评 |
|------|----------|------|
| 基础（位运算/离散化/随机） | 日常使用 | 9/10 |
| 数学（数论/组合/模运算） | 冲数论场 | 8/10 |
| 并查集家族 | 闭卷可写 | 9/10 |
| BIT + 线段树 | 高级 lazy 偶尔对照 | 7/10 |
| 主席树 | 会套板子 | 6/10 |
| 分块 | 入门四题全会 | 8/10 |
| ODT / ST表 / Trie / pb_ds | 板子熟练 | 8/10 |
| 图论（最短路/MST/LCA） | 闭卷可写 | 8/10 |
| 字符串 | 只有 KMP + 哈希 | 5/10 |
| DP（线性/区间/背包/树形/状压） | 基础应用没问题 | 8/10 |
| 贪心/二分/搜索 | 直觉好 | 8/10 |
| 对拍/调试 | 全套流程熟练 | 9/10 |
| CF 综合 | 对应 CF 黄~橙段 | - |

---

*与实际代码仓库同步 · 只记真会的，不记看过的*